

```{r}
winrate = read.table("sample_winrate_B.txt")
head(winrate)
```

```{r}
plot(winrate$V1, winrate$V2, pch = 20, xlab = "Move Number", ylab = "Black Winrate", main = "AlphaGo vs Ke Jie(W) Game 2")
lines(winrate$V1, winrate$V2)
```

